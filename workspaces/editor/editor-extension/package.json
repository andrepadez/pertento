{
  "name": "editor-extension",
  "private": true,
  "version": "2024.06.24.3",
  "type": "module",
  "scripts": {
    "start": "source ../../../.env && bun x --bun vite preview --port 3002",
    "prebuild": "bun --env-file=../../../.env ./prebuild.ts",
    "build": "source ../../../.env && bun x --bun vite build",
    "postbuild": "bun --env-file=../../../.env ./postbuild.ts"
  }
}
