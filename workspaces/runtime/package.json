{
  "name": "pertento-runtime",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "source ../../.env && bun x --bun vite build",
    "postbuild": "bun run --env-file=../../.env ./postbuild.ts",
    "dev": "bun run --env-file=../../.env --watch src/index.ts",
    "start": "bun run --env-file=../../.env src/index.ts"
  }
}
